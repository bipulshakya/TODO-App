# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=todo_app

# Or use a single connection string (takes priority):
# DATABASE_URL=mysql://user:password@host:3306/todo_app

# Server
PORT=5001

# Auth
JWT_SECRET=your_super_secret_key_here
